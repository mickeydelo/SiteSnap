{
  "name": "sitesnap",
  "version": "1.0.0",
  "description": "Local configurable screenshot studio for responsive pages and interactive states",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "SITESNAP_DEBUG=1 node index.js",
    "setup": "npm install && npx playwright install chromium",
    "check": "node --check index.js && node --check core/actions.js && node --check core/browser.js && node --check core/capture.js && node --check core/runner.js && node --check core/utils.js && node scripts/check-vercel.js",
    "check:vercel": "node scripts/check-vercel.js"
  },
  "dependencies": {
    "@sparticuz/chromium": "^149.0.0",
    "@vercel/blob": "^2.8.0",
    "archiver": "^6.0.0",
    "express": "^4.19.0",
    "playwright": "^1.58.0",
    "playwright-core": "^1.62.1"
  }
}
